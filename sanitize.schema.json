{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/lialh4qwq/opencode-sanitizer/main/sanitize.schema.json",
  "title": "opencode-sanitizer configuration",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "$schema": {
      "type": "string",
      "description": "Reference to this schema file."
    },
    "defaultPlaceholderHint": {
      "type": "string",
      "default": "6f1a3c8e-2b47-4d90-a15f-9c3e7b0d8214",
      "description": "Hint used by rules that do not set their own placeholderHint. Each match is replaced by <HINT:HASH>. Defaults to an opaque, meaningless identifier so the model is less tempted to decode it; use something non-descriptive."
    },
    "rules": {
      "type": "object",
      "description": "Map of rule name to rule. The key is the name used in logs. Rules whose key collides with one from the other config file are taken from the working-directory config.",
      "additionalProperties": {
        "type": "object",
        "additionalProperties": false,
        "required": ["pattern"],
        "properties": {
          "pattern": {
            "type": "string",
            "description": "JavaScript regular expression source, or an exact string when literal is true."
          },
          "flags": {
            "type": "string",
            "description": "Regular expression flags. The global flag (g) is always added."
          },
          "literal": {
            "type": "boolean",
            "default": false,
            "description": "Treat pattern as a literal string instead of a regular expression."
          },
          "placeholderHint": {
            "type": "string",
            "description": "Overrides defaultPlaceholderHint for this rule. Must not contain <, > or :."
          }
        }
      }
    }
  }
}
